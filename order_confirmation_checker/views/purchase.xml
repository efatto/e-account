<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="purchase_order_form" model="ir.ui.view">
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form" />
        <field name="arch" type="xml">
            <xpath expr="//sheet" position="inside">
                <button
                    name="button_check_attachment"
                    string="Check attachment with confirmation info"
                    type="object"
                />
                <field
                    name="attachment_to_check_id"
                    options="{'no_create': True}"
                    domain="[('res_model', '=', 'purchase.order'), ('res_id', '=', active_id)]"
                />
            </xpath>
            <xpath
                expr="//field[@name='order_line']/tree/field[@name='sequence']"
                position="after"
            >
                <field name="check_result" widget="boolean" string="C.O." />
                <field name="check_message" optional="hide" />
            </xpath>
            <xpath expr="//field[@name='date_order']" position="after">
                <field name="check_result" />
            </xpath>
        </field>
    </record>

</odoo>
