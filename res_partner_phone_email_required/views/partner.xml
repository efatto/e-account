<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_partner_property_form_customer_ref" model="ir.ui.view">
        <field name="name">res.partner.property.form.customer.ref</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="account.view_partner_property_form"/>
        <field name="arch" type="xml">
            <field name="phone" position="attributes">
                <attribute name="attrs">{'required': [
                    ('parent_id', '=', False),
                    ('mobile', '=', False),
                    ('email', '=', False)]}
                </attribute>
            </field>
            <field name="mobile" position="attributes">
                <attribute name="attrs">{'required': [
                    ('parent_id', '=', False),
                    ('phone', '=', False),
                    ('email', '=', False)]}
                </attribute>
            </field>
            <field name="email" position="attributes">
                <attribute name="attrs">{'required': [
                    ('parent_id', '=', False),
                    ('mobile', '=', False),
                    ('phone', '=', False)]}
                </attribute>
            </field>
        </field>
    </record>

</odoo>
