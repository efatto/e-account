<?xml version="1.0" ?>
<odoo>

    <record id="view_move_form" model="ir.ui.view">
        <field name="model">account.move</field>
        <field
            name="inherit_id"
            ref="account.view_move_form"
        />
        <field name="arch" type="xml">
            <button name="action_post" position="after">
                <field name="is_fiscal_document_type_default" invisible="1"/>
            </button>
            <xpath expr="//button[@name='action_post'][last()]" position="attributes">
                <attribute name="attrs">{'invisible': ['|', '|', '|', ('state', '!=', 'draft'), ('auto_post', '=', True), ('move_type', '=', 'entry'), ('is_fiscal_document_type_default', '!=', True)]}</attribute>
            </xpath>
            <button name="action_post" position="after">
                <button name="action_post" string="Confirm" class="oe_highlight"
                    type="object" groups="account.group_account_invoice"
                    attrs="{'invisible': ['|', '|', '|', ('state', '!=', 'draft'), ('auto_post', '=', True), ('move_type', '=', 'entry'), ('is_fiscal_document_type_default', '=', True)]}"
                    confirm="Fiscal document type is not the default. Are you sure?"
                />
            </button>
        </field>
    </record>

</odoo>
